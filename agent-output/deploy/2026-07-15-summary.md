# Deploy package — 2026-07-15

`2026-07-15-upload.zip` = the full current `public_html/` folder, ready to
upload to cPanel File Manager. Nothing here has been uploaded — that step is
still manual (no FTP credentials available in this session).

## What changed since the last package (2026-07-11)

**Technical fixes from `agent-output/seo/2026-07-15-report.md`:**

- **Fixed** `tools.html`'s stale title-lock script — the inline
  `MutationObserver` script (was forcing the crawlable title back to
  "18 AI Tools...") now matches the real `<title>`. Both now read "21 AI
  Tools..." after this cycle's addition (see below).
- **Added `og:image`/`twitter:image`** pointing at
  `https://promptupatch.com/og-image.png` to all 40 real pages under
  `public_html/` (10 root pages, 20 tool pages, 10 blog posts — the Google
  site-verification stub was left untouched). Previously zero pages set
  either tag, so social shares (X, Slack, Facebook, Discord) were falling
  back to a blank/default card.
- **Added Grok and DeepSeek to the "Alternatives in Chat & Writing" section**
  on `tools/chatgpt.html`, `tools/claude.html`, and `tools/gemini.html`. Both
  new pages already linked out to these three; the relationship is now
  bidirectional.
- **Not fixed this cycle:** the "19 tools"/"nineteen tools" copy in
  `blog/we-tested-deep-links.html` and `blog/ai-tool-red-flags.html` — the
  report flagged this as a Content-agent copy pass (body/narrative text, not
  machine-readable metadata), so it's left for that agent rather than edited
  here.

**Content promoted from `drafts/` (see `drafts/2026-07-15-summary.md`):**

- **Added** `blog/chatgpt-vs-claude-vs-gemini.html` — new comparison post
  (SEO gap #1). Links to `tools/chatgpt`, `tools/claude`, `tools/gemini` and
  three existing blog posts; closes with a `/generate` CTA.
- **Added** `blog/which-ai-tools-are-actually-free.html` — new practical
  "free tiers" post (SEO gap #4). Links to `tools/chatgpt`, `claude`,
  `gemini`, `grok`, `deepseek`, and `blog/free-credits-philosophy`.
- **Added** `tools/heygen.html` — new tool page for the AI-avatar/talking-video
  category (SEO gap #3). **Verified independently before promoting**: fetched
  `https://www.heygen.com/` live this cycle and confirmed the product name
  ("HeyGen") and homepage URL match what the Content agent drafted — the
  Content agent's own open question about this is resolved, nothing looked
  off, so it was safe to promote as-is.
- **Updated** `sitemap.xml` — added all three new URLs with `lastmod`
  `2026-07-15`, `monthly` changefreq, matching priority conventions
  (`0.8` for the tool page, `0.7` for the two blog posts).
- **Updated** `tools.html` — added HeyGen to the "Video Generation" category
  list, bumped the page title/meta/OG/Twitter/JSON-LD tool count from 20 to
  21, and inserted HeyGen into the `CollectionPage` schema's `itemListElement`
  at position 14 (after Sora), renumbering the remaining items 15–21.
- **Updated** `index.html` — added HeyGen to the homepage's "Tools we route
  to" list, and prepended both new blog posts to "Latest from the blog".
- **Updated** `blog.html` — added both new posts to the top of the visible
  list and to the `Blog` JSON-LD's `blogPost` array.
- **Checked, no change needed**: `robots.txt` already allows `/tools/*` and
  `/blog/*` with no disallow rules that would block any of the three new
  pages.

## Pre-upload verification the human should do

1. **This cycle's fixes are all local-file-only** — no live server was
   touched (no FTP credentials available this session; `api/server.js` was
   not read or modified). The og:image/twitter:image and title-lock fixes
   exist only in this zip and the git history until someone uploads them.
2. Spot-check the rendered og:image card once live — `og-image.png` is
   referenced by absolute URL (`https://promptupatch.com/og-image.png`), so
   it will only resolve correctly after upload; a pre-upload local check
   can't confirm the live image loads.
3. Confirm `assets/index-BLFRjWRo.js` (the compiled SPA bundle, not touched
   this cycle) already recognizes "HeyGen" as a routable target for
   `/generate` before or shortly after this goes live — same caveat the
   2026-07-11 package flagged for Grok/DeepSeek. If it doesn't route HeyGen
   prompts correctly yet, the "Route a prompt to it" link on
   `tools/heygen.html` will look normal but silently misroute.
4. No page count discrepancies found: every file in `tools/` (21) and
   `blog/` (12) has a matching `sitemap.xml` entry and vice versa; every
   internal link touched this cycle was checked against the actual file list.

## Upload steps (same as `deploy-notes/DEPLOY.md`)

1. Upload the contents of this zip into `public_html` on the server,
   preserving the `tools/` and `blog/` folder structure. This **replaces**
   `index.html`, `tools.html`, `blog.html`, `sitemap.xml`, and all 40
   existing pages that gained `og:image`/`twitter:image` tags (i.e.
   effectively the whole `public_html/` tree), and **adds** three new files:
   `blog/chatgpt-vs-claude-vs-gemini.html`,
   `blog/which-ai-tools-are-actually-free.html`, `tools/heygen.html`.
2. Do not touch `public_html/api/` — nothing changed server-side, and this
   agent had no live-server access this cycle.
3. Verify after upload:
       curl -sI https://promptupatch.com/tools/heygen                        # expect 200
       curl -sI https://promptupatch.com/blog/chatgpt-vs-claude-vs-gemini    # expect 200
       curl -sI https://promptupatch.com/blog/which-ai-tools-are-actually-free  # expect 200
       curl -s https://promptupatch.com/tools | grep -o '21 AI Tools'        # confirm title fix landed
4. In Google Search Console: resubmit `sitemap.xml`, then URL-Inspect the
   three new pages and request indexing.
5. Once confirmed live, the Social agent can safely reference the two new
   blog posts and the HeyGen tool page in future posts.
